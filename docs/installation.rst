.. _installation:

.. include:: global.txt

Installation
============

This package is compatible with Python 2.7+. The only other
requirement is requests_.

For those on Linux/Unix Mac OS, the easiest route will be :command:`pip` or
:command:`easy_install`::

    pip install emailage
